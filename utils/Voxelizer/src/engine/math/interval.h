#ifndef INTERVAL_H
#define INTERVAL_H


class Interval
{
public:
    const float max;
    const float min;
    Interval(float mini, float maxi) : max(maxi), min(mini) {

    }
};

#endif // INTERVAL_H
